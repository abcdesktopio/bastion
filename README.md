# SSH bastion

SSH bastion to connect to a user pod from an external host.

## Parameters

| Name  | Type   | Description |
|-------|--------|-------------|
| SSHKEY | String | SSH keys to add to the authorized_keys file. comma (,) as separator |
